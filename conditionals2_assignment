Ques 1.Write a program to count the minimum number of notes in a given amount using the switch statement.
    #include<bits/stdc++.h>
    using namespace std;
    int main(){
        int x;
        cout<<"Entere the amount: ";
        cin>>x;
        switch(x>=2000)
        {
            case 1:
            cout<<"notes of 2000 = "<<(x/2000)<<endl;
            x-=(x/2000)*2000;
        }
        switch(x>=500)
        {
            case 1:
            cout<<"notes of 500 = "<<(x/500)<<endl;
            x-=(x/500)*500;
        }
    
        switch(x>=200)
        {
            case 1:
            cout<<"notes of 200 = "<<(x/200)<<endl;
            x-=(x/200)*200;
        }
    
        switch(x>=100)
        {
            case 1:
            cout<<"notes of 100 = "<<(x/100)<<endl;
            x-=(x/100)*100;
        }
        switch(x>=50)
        {
            case 1:
            cout<<"notes of 50 = "<<(x/50)<<endl;
            x-=(x/50)*50;
        }
        switch(x>=20)
        {
            case 1:
            cout<<"notes of 20 = "<<(x/20)<<endl;
            x-=(x/20)*20;
        }
        switch(x>=10)
        {
            case 1:
            cout<<"notes of 10 = "<<(x/10)<<endl;
            x-=(x/10)*10;
        }
        switch(x>=5)
        {
            case 1:
            cout<<"notes of 5 = "<<(x/5)<<endl;
            x-=(x/5)*5;
        }
        switch(x>=2)
        {
            case 1:
            cout<<"notes of 2 = "<<(x/2)<<endl;
            x-=(x/2)*2;
        }
        switch(x>=1)
        {
            case 1:
            cout<<"notes of 1 = "<<(x/1)<<endl;
            x-=(x/1)*1;
        }
        return 0;
    }


Ques 2. Predict the output: 
      #include<iostream>
      using namespace std;
      
      int main( ) {
          int a = 5, b, c ;
          b = a = 15 ;
          c = a < 15 ;
          cout << "a = " << a << ", b = " << b << " , c = "<< c ;
      return 0;
      }
Output:- a = 15, b = 15 , c = 0

  
Ques 3. Predict the output:
      #include<iostream>
      using namespace std;
      
      int main() {
          int x = 3 ;
          float y = 3.0 ;
          if (x == y)
          cout <<"x and y are equal"  ;
          else
          cout << "x and y are not equal"  ;
          return 0;
      }
Output:- x and y are equal


Ques 4. Predict the output:
      #include<iostream>
      using namespace std;
      
      int main(){
          int test = 0;
          cout << "First character " << '1' << endl;
          cout << "Second character " << (test ? 3 : '1') << endl;
          return 0;
      }
Output:- First character 1
         Second character 49


Ques 5. Predict the output:
      #include <iostream>
      using namespace std;
      int main(){
          int a = 18; int b = 12;  
          bool t = (a > 20 && b < 15)? true : false;
          cout <<"Value of t: " << t ;
          return 0;
      }
Output:- Value of t: 0


Ques 6. Predict the output:
      #include <iostream>
      using namespace std;
      int main() {
        int number = -4;
        char result;
        result = number > 0 ? 'P' : 'N';
        cout << result << endl;
        return 0;
      }
Output:- N
